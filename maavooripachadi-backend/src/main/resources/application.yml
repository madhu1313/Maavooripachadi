server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/mavooripachadi}
    username: ${DB_USER:root}
    password: ${DB_PASS:Mad@1234}
    driver-class-name: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.jdbc.batch_size: 50
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: 'classpath:db/migration'

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

payments:
  razorpay:
    key: ${RAZORPAY_KEY_ID:rzp_test_RUbsNadTP2WNOd}
    secret: ${RAZORPAY_KEY_SECRET:56XTLHV6EQ7FdTJQPKWu6PvK}
    base-url: ${RAZORPAY_BASE_URL:https://api.razorpay.com/v1}

app:
  security:
    allowed-origins:
      - http://localhost:4200
      - https://maavooripachadi.com
      - https://www.maavooripachadi.com
